- type: serial
  steps:
    - service: builder
      command: npm i

    - service: builder
      command: npm test

    - service: builder
      command: npm run build
      
    - service: builder
      tag: '^(v[0-9]+\.[0-9]+\.[0-9]+)$'
      command: ./publish.sh


